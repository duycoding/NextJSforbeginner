'use client';
import React from 'react'

export const AddToCart = () => {
  return (
    <div>
        <button className='btn btn-primary' onClick={() => console.log('Add to cart')}>Add to cart</button>
    </div>
  )
}
